# KACHINOVA — DESIGN SYSTEM

> 目標：「不動産会社がテック風のデザインを採用したサイト」ではなく、
> 「テクノロジー企業が不動産という産業を再設計している」と感じられること。

すべて `assets/css/kachinova.css` の 1 ファイルに定義されています。

---

## 1. カラー

無彩色が基調。色は**技術を語る箇所にだけ、光として**使います。

| トークン | 値 | 用途 |
|---|---|---|
| `--ink` | `#05070a` | ページの地。すべての基準 |
| `--charcoal` / `--graphite` / `--slate` | `#0e1216` / `#171c21` / `#262d34` | 面の微差 |
| `--steel` | `#767f88` | 最も暗い文字色（`--ink` 上で 4.8:1 = AA 合格の下限） |
| `--silver` | `#8d979f` | 補助文字 |
| `--mist` | `#c3cbd1` | 写真上の本文 |
| `--ice` | `#dfe7ec` | 主文字色 |
| `--paper` | `#f2f1ee` | 反転セクションの地（暖かい生成り） |
| `--cyan` | `#4fe3ff` | **アクセント。技術・データ・進行を示す箇所のみ** |
| `--gold` | `#c4a870` | ロゴ由来のシャンパンゴールド。「ESTATE」側の声。細い罫と小さなラベルのみ |

**ルール**
- アクセント（cyan / gold）の面積は画面の 1〜2% を超えない。
- 青い面を作らない。青は線・点・光としてのみ登場する。
- 反転（`--paper`）セクションでは `--steel` を `#5f676e`、cyan を `#0e7f9b` に差し替え、
  紙面上でも AA を維持する（`.section--light` 内で自動）。

## 2. タイポグラフィ

| 役割 | フォント | 用途 |
|---|---|---|
| `--f-display` | **Bodoni Moda** | 欧文の大見出し。映画のタイトルカードの声。常に大文字 |
| `--f-jp` | **Noto Serif JP** | 和文の大きな一文。明朝＝「住まい」の声 |
| `--f-sans` | **Noto Sans JP** | 本文 |
| `--f-mono` | **IBM Plex Mono** | ラベル・番号・ステータス。＝「テクノロジー」の声 |

**2層見出し**：欧文ディスプレイ（大） × 和文明朝（小）を必ずセットで置く。
片方だけにしない。これがブランドの声の骨格です。

スケールはすべて `clamp()`。和文は同ポイントでも字面が大きいため専用トークン `--t-jp` を持ちます。

```
--t-mega  clamp(2.5rem, 9.6vw, 8.6rem)   DESIGN. / TECHNOLOGY. / 最終カット
--t-hero  clamp(2.4rem, 7.2vw, 7rem)     ヒーロー見出し
--t-xxl   clamp(2.2rem, 6.2vw, 5.2rem)   セクション見出し
--t-jp    clamp(1.7rem, 5.4vw, 4.4rem)   和文の大きな一文
```

和文は `word-break: auto-phrase`（対応ブラウザ）で文節単位に折り返します。

## 3. 連続する背景（継ぎ目を作らない）

**セクションは背景を持ちません。** ページ全体に 1 枚の `.bg-canvas`（fixed）を敷き、
その上に `.bg-grain`（SVG フラクタルノイズ / `mix-blend-mode: overlay` / opacity .05）を重ねます。

- 隣り合うセクションの間に色の段差が原理的に発生しない。
- グレインが淡い階調のバンディング（縞）を撹拌する。

反転セクション `.section--light` だけが例外で、`::before` で **上下 ±220px はみ出して**
紙色をフェードさせます。境界は線ではなく常に長いグラデーションです。

**禁止**：波形・斜め・三角の SVG ディバイダ／実線ボーダーによる区切り／浮かせるためのドロップシャドウ。

## 3b. 背景レイヤー — NY モダンアート

暗いセクションの背景に、ニューヨークの抽象・ミニマル絵画の語彙で描いた
**1枚の絵**を固定配置しています（`.bg-art`）。装飾ではなく、無地の黒に建築的な奥行きを与える層です。

| レイヤー | モチーフ | 内容 |
|---|---|---|
| `.bg-art__field` | **Ad Reinhardt / Donald Judd** | 黒の中の黒。ΔL* ≤ 3 のハードエッジ矩形。140秒かけて微かに移動 |
| `.bg-art__weave` | **Agnes Martin** | 94px 方眼の、ほとんど見えない手描き風グリッド。楕円マスクで縁を消す |
| `.bg-art__zips` | **Barnett Newman** | 画面を切る垂直の光の帯（1px）。1本はシャンパンゴールド。27秒／34秒で呼吸 |
| `.bw` | **Mondrian《Broadway Boogie Woogie》** | グリッド上を走る光。＝都市を流れるデータ |

**設計上の制約**

- **CSS のみ。** canvas も requestAnimationFrame も使いません。`transform` / `opacity` だけを動かすため
  合成のみで完結し、実測で **アイドル 120fps**。
- **「常に何かが動いている」状態を避ける。** 光の走行はサイクルの 40% で終わり、
  残り 60% は壁が静まります。周期は 21〜31 秒でバラバラ（等間隔だと絵ではなくローディングバーに見える）。
- **`prefers-reduced-motion: reduce` では動きだけが止まり、絵は残ります。** 光の粒は非表示。
- スマートフォンでは走る光を 6 本→3 本に間引き。
- `contain: strict` でクリップするため、横スクロールを一切発生させません。

映像セクション（`.film`）と反転セクション（`.section--light`）は自前の地を持つため、
この絵はページの**静かな箇所にだけ**現れます。タイルは半透明（`rgba(6,9,12,.66)`）にして、
背後の絵が薄く透けるようにしています。

## 4. モーション

| 項目 | 値 |
|---|---|
| 標準イージング | `cubic-bezier(.22, 1, .36, 1)` |
| 入場アニメ | 1100ms（`.reveal` = opacity + translateY + blur） |
| スタッガー | 80ms（`--i` を JS が自動採番） |
| マスクリビール | 1150ms / 遅延 90ms（`.line-mask`） |

- アニメーションは `transform` / `opacity` / `filter` のみ。
- 同時に始動する要素は 3 つまで（スタッガーで散らす）。
- **`prefers-reduced-motion: reduce` ですべて無効化**し、同時に動画の読み込みも止める。
- 禁止：パーティクル、キラキラ、300ms 以下、bounce / elastic、スクロールジャック。

### 署名モーション — THE REVALUE LINE

サイト全体で**キー演出は 1 つだけ**。
「既存の間取り（細線）の上に、新しい設計線（アクセント色）が自ら描かれていく」SVG です。

コンセプト（＝ RE-ENGINEERING）を動詞として翻訳したもので、
`RE:DESIGN` と `SMART LIVING` の 2 か所で、**同じ線・違うノード**として反復します。

実装：`src/components.mjs` の `plan()` ＋ `.plan-new` の `stroke-dashoffset`。
パス長は JS が `getTotalLength()` で実測して `--len` に入れるため、
どんな図面に差し替えても描画速度が一定に保たれます。

## 5. コンポーネント

| クラス | 役割 |
|---|---|
| `.hero` / `.hero--inner` | 100svh のフルスクリーン映像 ／ 下層ページ用の 74svh 版 |
| `.film` | 動画を背景に持つシネマティックセクション（6本用） |
| `.film__scrim` | 写真の上で文字を読ませるためのグラデーション。**最も明るいカットに合わせて設計** |
| `.statement` | 巨大な一文だけのセクション（スクロールの「間」） |
| `.flow` / `.flow__step` | ACQUIRE → … → REVALUE の工程表示 |
| `.value` | 3つの価値。カードではなく 88svh の大胆な面 |
| `.tiles` / `.tile` | 1px の線で区切ったグリッド（角丸を使わない） |
| `.chip--live / --dev / --research / --future / --todo` | 開発ステータス／DATA_REQUIRED の明示 |
| `.plan` | 署名モーション（上記） |
| `.marquee` | 余白を殺さず動きを与える帯 |
| `.data` | 会社概要・FAQ の定義リスト |
| `.btn` | 下から塗りが上がってくるボタン。角丸なし |

**避けているもの**：角丸カードの量産、過剰なアイコン、安いグラデーション、
物件検索サイト風の UI、青い企業サイト的配色。

## 6. アクセシビリティ

- 写真の上以外のテキストは全ページ **WCAG 2.1 AA（4.5:1）** を満たす（`--steel` がその下限）。
- セマンティック HTML。`section` には必ず `aria-labelledby`。
- キーボード：スキップリンク、`:focus-visible` の cyan アウトライン、Esc でドロワーを閉じる。
- 動画は `aria-hidden="true"` + `tabindex="-1"`。**情報は必ずテキストにも存在する。**
- 動画が無くても（読み込み失敗・省データ・reduced-motion）内容は完全に理解できる。
